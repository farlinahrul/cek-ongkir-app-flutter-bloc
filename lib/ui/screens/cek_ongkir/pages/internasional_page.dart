import 'package:flutter/material.dart';

class InternasionalPage extends StatelessWidget {
  const InternasionalPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.directions_transit, size: 350);
  }
}
