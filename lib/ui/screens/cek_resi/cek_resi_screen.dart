import 'package:flutter/material.dart';

class CekResiPage extends StatelessWidget {
  const CekResiPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Cek Resi Page"),
      ),
    );
  }
}
